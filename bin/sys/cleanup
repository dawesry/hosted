#!/bin/zsh

if rm -f ~/.p10k.zsh.old; then
    echo "Deleted ~/.p10k.old succeeded"
else
    echo "No ~/.p10k.old found"
fi

if rm -rf ~/bin.old; then
    echo "Deleted ~/bin.old succeeded"
else
    echo "No bin found"
fi